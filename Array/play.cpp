#include<iostream>
#include<bits/stdc++.h>
#include<algorithm>
using namespace std;
// #include <iostream>

// #include <algorithm>

// #include <vector>

// using namespace std;

// int main() {
//     int a = 20; // Initializing value to be searched
//     vector<int> vec = {10, 20, 30, 40, 50}; 

//     //find() function is invoked to search a inside the vector
//     auto res = find(vec.begin(), vec.end(), a);

//     //if element is found in the given search space i.e, vector
//     if (res != end(vec)) {
//         cout << "Element " << a << " is found " << endl;
//     }
//     a = 35;

//     res = find(vec.begin(), vec.end(), a);

//     //if element is not found in given search space i.e, vector 
//     //then find() function will point to the end 
//     if (res == end(vec)) {
//         cout << "Element " << a << " is not found" << endl;
//     }
//     return 0;
// }
int main()
{
    //vector<int>v={1,2,3};
    //cout<<*find(v.begin(),v.end(),100);
    // vector<vector<int>>v(3,vector<int>(4));
    // cout<<v.size()<<" "<<v[0].size()<<endl;
    int a=-7,b=3;
    cout<<a%b<<endl;
}